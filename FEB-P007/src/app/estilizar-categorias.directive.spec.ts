import { EstilizarCategoriasDirective } from './estilizar-categorias.directive';

describe('EstilizarCategoriasDirective', () => {
  it('should create an instance', () => {
    const directive = new EstilizarCategoriasDirective();
    expect(directive).toBeTruthy();
  });
});