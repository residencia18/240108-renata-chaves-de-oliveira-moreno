import { FiltroSuinosPipe } from './filtro-suinos.pipe';

describe('FiltroSuinosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSuinosPipe();
    expect(pipe).toBeTruthy();
  });
});
